general {
	interval = 5
        output_format = i3bar
}

order += "path_exists VPN"
order += "ethernet _first_"
order += "wireless _first_"
order += "memory"
order += "disk /"
order += "tztime holder__hey_man"
order += "tztime holder_cputemp"
order += "volume master"
order += "battery 0"

tztime Tehran {
        format = "%Y-%m-%d %H:%M:%S"
	timezone = "Asia/Tehran"
}

tztime holder__hey_man {
        format = "holder__hey_man"
}

tztime holder_cputemp {
        format = "holder_cputemp"
}

memory {
	format = "Mem: %available"
        threshold_degraded = "10%"
        format_degraded = "MEMORY: %free"
}

volume master {
        format = "♪: %volume"
        format_muted = "♪: muted (%volume)"
        device = "default"
}

path_exists VPN {
	path= "/proc/sys/net/ipv4/conf/Meta"
	format = "VPN"
	format_down = ""
}

disk "/" {
        format = "Df: %free"
}

battery 0 {
        format = "%status %percentage"
        format_down = ""
        status_chr = "⚡ CHR"
        status_bat = "🔋 BAT"
}

wireless _first_ {
        format_up = "Wi-Fi: (%quality at %essid, %bitrate) %ip"
        format_down = "Wi-Fi: Down"
}
ethernet _first_ {
	format_up = "Eth: %ip (%speed)"
        format_down = "E: down"
}


